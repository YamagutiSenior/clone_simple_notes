apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: access-echo
spec:
  podSelector:
    matchLabels:
      app: echo
  ingress:
  - from:
    - podSelector:
        matchLabels:
          access: "true"
