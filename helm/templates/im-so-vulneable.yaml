apiVersion: apps/v1
kind: Deployment
metadata:
  name: vulnerable
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vulnerable
  template:
    metadata:
      labels:
        app: vulnerable
    spec:
      containers:
      - name: vulnerable
        image: nginx:1.14
        ports:
        - containerPort: 80
